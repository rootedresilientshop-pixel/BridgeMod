# DreamCraft: Legacies v2 — Task Log

_Last updated: 2026-02-16_

---

## Completed

| Date | Task | Outcome |
| :--- | :--- | :--- |
| 2026-02-15 | Pi 5 SSD reformatted exFAT → EXT4 | `/dev/sda2` labelled `DREAM_VAULT`, mounted at `/mnt/ssd`, 870 GB free. fstab entry persisted by UUID. |
| 2026-02-15 | Docker Engine installed on Pi 5 | v29.2.1, daemon active, overlayfs driver. `passivepi` added to docker group. |
| 2026-02-15 | SSH Trust Bridge established (Pi 5 → Pi 4) | `passivepi@intelpi` ed25519 key appended to Pi 4 `authorized_keys`. Live test confirmed. |
| 2026-02-15 | AITrust service stopped & disabled on Pi 5 | Freed ~565 MB RAM. Service will not restart on reboot. |
| 2026-02-16 | Makefile created in project root | `up`, `down`, `logs`, `health`, `vault-sync`, `backup` targets. Hard-tab verified. `ORACLE_HOST` reads from `.env` automatically. |
| 2026-02-16 | Project documentation created | `project.md`, `state.md`, `tasks.md`, `decisions.md` written reflecting Hybrid Cloud-Sovereign architecture. |
| 2026-02-15 | Infrastructure audit (Pi 4 + Pi 5) | Cloudflare tunnel config mapped. Port 5000 origin identified. All service states documented. |
| 2026-02-13 | DreamCraft v2 simulation core scaffolded | Engine, API, LLM client, schema, tests (~2007 lines). |
| 2026-02-13 | Character loader + Scribe service implemented | `character_loader.py`, `scribe_service.py`, `vault_manager.py`. |
| 2026-02-13 | Docker container configured (linux/arm64) | `Dockerfile`, `docker-compose.yml`, `entrypoint.sh`, healthcheck. |
| 2026-02-13 | LLM-driven emergent simulation | Characters make decisions via Ollama API; narrative generated by Scribe. |

---

## Upcoming — Immediate Priority

| # | Task | Depends On | Target Node |
| :--- | :--- | :--- | :--- |
| 1 | Provision Oracle Cloud Ampere A1 instance (4 vCPU / 24 GB, free tier) | — | Oracle |
| 2 | Install Docker Engine on Oracle instance | Task 1 | Oracle |
| 3 | Install Ollama on Oracle + pull `llama3.2:3b-instruct-q4_K_M` and `phi` | Task 1 | Oracle |
| 4 | Configure `.env` with `SAGA_LLM_ENDPOINT=http://localhost:11434/v1` and Oracle host IP | Task 1 | Dev |
| 5 | Deploy DreamCraft v2 Docker container on Oracle (`make up`) | Tasks 2–4 | Oracle |
| 6 | Add Oracle SSH host key to Pi 5 `known_hosts` | Tasks 1, 5 | Pi 5 |
| 7 | Run `pi5_pull_vault.py --install-cron` on Pi 5 to schedule daily vault sync | Tasks 5–6 | Pi 5 |
| 8 | Update Pi 4 Cloudflare tunnel config: replace `intelpi.local:5000` with the DreamCraft v2 dashboard endpoint | Task 5 | Pi 4 |

---

## Upcoming — Maintenance

| # | Task | Priority | Node |
| :--- | :--- | :--- | :--- |
| M1 | Debug `passivebrain.service` crash (exit-code 2) — run `journalctl -u passivebrain -n 50` | High | Pi 4 |
| M2 | Plug ethernet cable into Pi 4 (eth0 currently DOWN — WiFi-only is fragile for a public gateway) | Medium | Pi 4 |
| M3 | Identify unknown service on Pi 5 port :20241 | Medium | Pi 5 |
| M4 | Identify unknown service on Pi 5 port :5000 (publicly routed via cloudflared) | Medium | Pi 5 |
| M5 | Update dev SSH config: `dreamcraft-pi4` block — change `User claude` to `User passivepi` | Low | Dev |
| M6 | Remove or disable nginx on Pi 4 (currently FAILED, port conflict with Apache) | Low | Pi 4 |
| M7 | Add Oracle Cloud instance to Pi 4 and Pi 5 `known_hosts` for vault-sync pipeline | Low | Pi 4 + Pi 5 |
